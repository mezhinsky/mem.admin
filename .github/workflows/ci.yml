name: CI

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  build:
    runs-on: ubuntu-latest

    permissions:
      contents: read
      packages: write

    # ВАЖНО: env объявлен на уровне job
    env:
      VITE_API_BASE_URL: https://api.6284765-xq71389.twc1.net

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Build (local check)
        run: npm run build

      - name: Debug env
        run: echo "VITE_API_BASE_URL=$VITE_API_BASE_URL"

      - name: Login to GitHub Container Registry
        if: github.event_name == 'push'
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push Docker image
        if: github.event_name == 'push'
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: |
            ghcr.io/${{ github.repository_owner }}/mem.admin:latest
            ghcr.io/${{ github.repository_owner }}/mem.admin:${{ github.sha }}
          labels: |
            org.opencontainers.image.source=${{ github.repository }}
          build-args: |
            VITE_API_BASE_URL=${{ env.VITE_API_BASE_URL }}
